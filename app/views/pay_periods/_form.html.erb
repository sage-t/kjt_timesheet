<%= form_with(model: pay_period, local: true) do |form| %>
  <% if pay_period.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pay_period.errors.count, "error") %> prohibited this pay_period from being saved:</h2>

      <ul>
      <% pay_period.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start %>
    <%= form.date_select :start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.date_select :end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
